const app = require("express")()

app.get("/", (req, res)=> {

    console.log(req);
for (let index = 0; index < array.length; index++) {
    const element = array[index];
        res.send("res") 
}
})

app.listen(4545, ()=>console.log("Receiving on 4545"))
